export enum AssetState {
  new = "new",
  almostUsed = "almost used",
  used = "used",
  damaged = "damaged",
  obsoletedDecommissioned = "obsoleted decommissioned",
  obsoletedDonated = "obsoleted donated",
  obsoletedDiscarded = "obsoleted discarded",
}

export enum AssetActive {
  true = "Active",
  false = "Inactive",
}

export enum ExpensesStatus {
  using = "using",
  discarded = "discarded",
}
